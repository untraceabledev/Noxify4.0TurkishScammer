function getCrypto(pCharacterId)
    -- todo
end

function transferCryptoByPhone(pCharacterId, pTargetId, cryptoType, pAmount)
    -- todo
end

function useCrypto(pCryptoId, pAmount)
    -- todo
end

function addCrypto(pCryptoId, pAmount)
    -- todo
end

function purchaseCryptoByPhone(pCharacterId, pAccountId, cryptoType, pAmount)
    -- todo
end